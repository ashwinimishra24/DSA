#include <iostream>
using namespace std;

int main()
{
    // Question 1
    // float f = 10.5;
    // float p = 2.5;
    // float *ptr = &f;
    // (*ptr)++;
    // *ptr = p;
    // cout<<f<<endl;
    // cout <<*ptr<<" "<<f<<" "<<p;

    // Question 2
    // int a = 7;
    // int b = 17;
    // int *c = &b;
    // *c = 7;
    // cout << a <<" "<<b<<endl;

    // Question 3
    // int *ptr = 0;
    // int a = 10;
    // *ptr = a;
    // cout<<*ptr<<endl;

    // Question 4
    // int a[3] = {1, 2, 3};
    // cout << *(a + 2)<<endl;

    // Question 5
    // int arr[] = {4, 5, 6, 7};
    // int *p = (arr + 1000000);
    // cout << *arr + 9<<endl;

    // Question 6
    // char s[]= "hello";
    // char *p = s;
    // cout << s[0] << " " << p[0]<<endl;

    // Question 7
    // char *ptr;
    // char Str[] = "abcdefg";
    // ptr = Str;
    // ptr += 5;
    // cout << ptr <<endl;

    // Question 8
    // int numbers[5];
    // int *p;
    // p = numbers;
    // *p = 10;
    // p = &numbers[2];
    // *p = 20;
    // p--;
    // *p = 30;
    // p = numbers + 3;
    // *p = 40;
    // p = numbers;
    // *(p + 4) = 50;
    // for (int n = 0; n < 5; n++)
    // {
    //     cout << numbers[n] << ",";
    // }

    // Question 9
    // float arr[5] = {12.5, 10.0, 13.5, 90.5, 0.5};
    // float *ptr1 = &arr[0];
    // float *ptr2 = ptr1 + 3;
    // cout << *ptr2 << " ";
    // cout << ptr2 - ptr1<<endl;

    return 0;
}